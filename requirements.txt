# Streamlit for app interface
streamlit==1.25.0

# Langchain for RAG implementation
langchain==0.0.305

# Transformers for GPT-4 model integration
transformers==4.34.0

# Sentence transformers for embeddings
sentence-transformers==2.2.2

# BM25 for ranking
rank-bm25==0.2.2

# OpenAI for GPT integration
openai==0.28.0  # Updated to avoid RateLimitError import issue

# Keyword extraction
keybert==0.8.1

# Natural Language Toolkit (NLTK)
nltk==3.8.1

# Spacy for NLP tasks
spacy==3.6.0  # Downgrade to 3.6.0 to avoid recent issues with 3.6.1

# Scikit-learn for machine learning models
scikit-learn==1.2.2

# PyMuPDF for PDF processing
PyMuPDF==1.23.7

# Language tool for grammar checking
language-tool-python==2.7.1

# Core dependencies
thinc==8.1.10  # To match spacy version and avoid issues with 8.2.x
torch==2.0.1  # To ensure compatibility with spacy and transformers

# Optional dependencies for compatibility
Cython==0.29.36
numpy==1.24.3

# Other dependencies based on your code
python-dotenv==1.0.0  # For loading environment variables
requests==2.31.0  # For HTTP requests (used in PDF download function)
